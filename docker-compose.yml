version: '3'
services:
  nginx:
    depends_on:
      - wkddngus5/jwp-trello
    image: jwp/nginx
    links:
      - wkddngus5/jwp-trello:jwp_trello
    ports:
      - "80:80"
  jwp-trello:
    image: jwp-trello
    links:
      - db
    environment:
      - "SPRING_PROFILES_ACTIVE=dev"
  db:
    image: mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: roalclsrn
      MYSQL_USER: dev
      MYSQL_PASSWORD: roalclsrn
      MYSQL_DATABASE: jwp-trello
    volumes:
      - trello-datavolume:/var/lib/mysql
volumes:
  trello-datavolume:
